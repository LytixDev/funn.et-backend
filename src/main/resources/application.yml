server.port: [INSERT_SERVER_PORT_HERE]

spring:
  datasource:
    url: jdbc:mysql://localhost:[INSERT_PORT_HERE]/[INSERT_ROUTE_HERE]/ 
    username: [INSERT_USERNAME_HERE]
    password: [INSERT_PASSWORD_HERE]
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true